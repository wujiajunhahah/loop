# Loop — 香港 Physical Hackathon 项目背景与协作上下文（V2）

> 更新依据：2026-08-01 最新项目会议纪要  
> 当前阶段：软件优先的 MVP 设计与并行开发

## 1. 项目背景

Loop 是一个准备参加香港 Physical Hackathon 的生命记录与情感陪伴项目。

项目关注一个重大但不希望被沉重表达的人生命题：人终将缺席。很多人在真正面对失去之前，并不会意识到应该系统地留下声音、习惯、故事、关系细节和想说的话。普通录音笔、相册或聊天记录能够保存数据，却很难完成有对象、有筛选、有编排、有仪式感的关系传递。

Loop 不回避死亡，也不把死亡包装成持续的悲伤体验。产品希望用轻盈、日常、可接近的方式，让记录者提前思考：

> 当我无法继续亲自表达时，什么仍然代表我？  
> 我希望把它留给谁？  
> 我希望这些内容未来如何被感受到？

产品不承诺让接收者“走出来”，不扮演救世主，也不替代现实关系或专业支持。它提供的是记录者提前建立的生命轨迹，以及由接收者自主决定是否靠近的陪伴。

---

## 2. 核心产品定义

> Loop 是一套以“人终将缺席”为底层命题，帮助记录主体采集、整理和编排个人生命轨迹，并为特定关系生成可被托付和演绎的对象专属 Agent 的系统。

产品的核心不是游戏，也不是硬件本身，而是：

1. **生命轨迹记录**：采集真实的多模态 Context；
2. **关系化编排**：区分“我是谁”和“我与你之间是什么”；
3. **对象专属 Agent**：面向不同接收者使用不同内容和表达边界；
4. **未来陪伴演绎**：在用户允许的时机，通过回忆、回应、祝福或共同计划呈现；
5. **实体托付**：硬件提供专属感、传承感和仪式入口。

它不是：

- 普通录音笔或云相册；
- 完全自由的人格克隆；
- 默认主动骚扰用户的设备；
- 自动判断悲伤并干预人生的产品；
- 以“养成逝者”为叙事的游戏。

---

## 3. 用户角色与主场景

### 3.1 角色

系统至少区分三个角色：

- **购买者**：购买产品或服务的人；
- **记录者 / 编排者**：输入、筛选、审核并授权内容的人；
- **接收者 / 使用者**：未来与关系 Agent 互动的人。

购买者与记录者可能相同，也可能不同。

### 3.2 MVP 主场景

当前最优先、最顺畅的主场景是：

> 记录主体自己购买、自己记录、自己编排，并把内容留给一个具体的人未来使用。

衍生场景包括：

- 子女购买并协助父母记录；
- 家庭成员替不擅长使用软件的人完成编辑；
- 记录者为多个对象分别建立不同关系 Agent。

MVP 只跑通一对一关系，不同时实现完整家庭网络。

---

## 4. Context 收集与编辑

### 4.1 主动录入是核心

主动录入包括：

- 语音；
- 文字；
- 照片；
- 视频；
- 对具体问题的回答；
- 对特定关系的祝福、回忆和共同计划。

主动录入必须由软件进行引导，不能只提供一个空白录音按钮。

### 4.2 无感录入是补充

无感录入可能包括：

- 时间与日期；
- 地点；
- 天气；
- 日常活动数据；
- 可穿戴设备数据；
- 其他经授权的行为数据。

这些信息通常碎片化严重，只能作为 Context 辅助，不应直接作为人格内容。

### 4.3 情绪只作为权重

情绪信息可以帮助判断内容的重要程度或呈现优先级，但不能被包装成可靠的“读心”。

推荐字段：

- emotion_label：用户自述或模型建议；
- emotion_intensity：内容的情绪强度；
- importance_weight：内容的重要性；
- sensitivity_level：呈现风险；
- confidence：模型判断可信度。

### 4.4 关系化引导

App 根据接收关系生成不同提问模板，例如：

- 父母 → 子女；
- 伴侣 → 伴侣；
- 朋友 → 朋友；
- 祖辈 → 晚辈。

提问应帮助用户回答：

- 什么最能代表我；
- 这件事为什么只属于我们；
- 我希望对方在什么情况下看到；
- 我希望系统如何演绎；
- 哪些内容不能被 AI 改写；
- 哪些内容可以被 AI 进行有限整理。

### 4.5 原文与 AI 层必须分开

系统必须保留原始内容，并把 AI 处理结果作为独立派生层：

```text
原始素材
→ 转录 / 标签
→ AI 摘要
→ 关系编排
→ 生成式回应
```

用户必须能够：

- 查看原文；
- 修改标签；
- 调整对象；
- 删除 AI 推断；
- 审核生成内容；
- 设置 AI 使用权限。

---

## 5. Agent 设计

### 5.1 关系拆分

同一个记录主体可以有多个关系 Agent：

- 留给孩子的 Agent；
- 留给伴侣的 Agent；
- 留给朋友的 Agent。

每个 Agent 只能读取：

- 被授权的公共生命 Context；
- 对该接收者开放的关系专属 Context；
- 该关系下的计划与表达规则。

严禁跨对象泄露。

### 5.2 Agent 的三种输出模式

#### A. 原始内容呈现

直接播放或展示真实录音、图片、视频和文字。

#### B. 基于原文的编排

AI 可以进行摘要、串联、检索和情境化呈现，但必须保留来源。

#### C. 有边界的人设回应

在记录者明确授权时，AI 可以基于原始素材生成符合表达风格的回应，但必须：

- 明确标记为 AI 生成；
- 能追溯到使用了哪些 Context；
- 不虚构新的事实；
- 不代表记录者作出新的重大决定；
- 不发布医疗、法律、财务等高风险建议；
- 不强迫接收者完成任务；
- 不跨越记录者设置的禁区。

因此，产品原则从绝对的“不生成”更新为：

> 可以演绎，但必须有来源、有授权、有边界、有标记。

### 5.3 Context 管理能力

Agent 至少需要：

- public context；
- relationship-specific context；
- private context；
- importance / emotion weight；
- trigger policy；
- generation policy；
- provenance；
- content sensitivity；
- recipient permissions。

---

## 6. 陪伴演绎与触发

### 6.1 陪伴层级

产品可以包含：

1. **被动陪伴**：静态遗留物、内容收藏、回忆浏览；
2. **主动交互**：用户主动打开并与 Agent 互动；
3. **情境提示**：在用户授权的场景下给出轻量入口。

### 6.2 触发不是随机

触发必须来自可解释条件：

- 用户主动打开；
- 生日、纪念日、节日；
- 指定时间；
- 指定地点；
- 天气变化；
- 共同计划进度；
- 用户主动反馈的当前状态。

禁止把完全随机推送当成核心能力。

### 6.3 主动性必须分级

推荐策略：

```text
pull_only
scheduled_opt_in
contextual_suggestion
proactive_allowed
```

默认使用 `pull_only`。

即使系统检测到某种状态，也应先提供轻提示，不直接播放强情绪内容。强主动干预不属于 MVP。

### 6.4 “远行信使”作为表现层

游戏化不是核心价值，但可以作为情感演绎的形式。

当前可探索的概念：

> 用户与关系 Agent 的每次互动形成一张明信片、一封远行信或一个纪念节点。

它的作用是：

- 让长期互动可见；
- 形成仪式感；
- 避免纯聊天界面的空洞；
- 让共同计划产生积累。

### 6.5 用户成长体系

不要使用“养成逝者”的叙事。

可以设计为：

> 用户成长体系 / 关系轨迹成长

用户反馈可以调整：

- 内容呈现频率；
- 表达长度；
- 偏好的媒介；
- 允许的主动程度；
- Agent 的互动方式。

注意：被调整的是用户体验与关系呈现，不是随意改变记录者人格。

---

## 7. 软件与硬件优先级

### 7.1 当前结论

当前阶段：

> 软件优先，硬件后置但保留接口。

软件必须先跑通：

- Context 收集；
- AI 编辑；
- 关系拆分；
- 对象专属 Agent；
- 演绎和反馈；
- 远行信 / 明信片式结果。

### 7.2 硬件角色

戒指仍然可以作为：

- 纪念物；
- 专属物；
- 身份钥匙；
- 触发入口；
- 后续音频或生理数据采集设备。

但当前不应让软件闭环依赖：

- 戒指直接对话；
- HRV 准确识别情绪；
- 完整的无感记录；
- 复杂硬件交互。

MVP 使用 Hardware Simulator 或简单按钮 / NFC / BLE 事件即可。

---

## 8. 更新后的 MVP

### 8.1 软件主闭环

1. 记录主体创建自己的生命档案；
2. 选择一个接收者及关系；
3. App 使用关系化问题引导输入；
4. 上传一段原始语音、图片或文字；
5. AI 进行转录、分类、情绪权重和关系标签建议；
6. 用户审核、修改并设置 AI 演绎权限；
7. 系统生成该接收者专属 Agent；
8. 接收者主动进入体验；
9. Agent 根据真实 Context 呈现回忆或有限回应；
10. 本次互动形成一张“远行明信片”；
11. 用户给出反馈，系统调整未来呈现方式。

### 8.2 Physical 层

可通过以下任一方式触发软件体验：

- 戒指触摸模拟器；
- NFC 标签；
- BLE 按钮；
- 桌面实体物件；
- 软件内模拟事件。

硬件不是软件 Demo 的阻塞项。

### 8.3 推荐 Demo 故事

> 一位母亲为女儿记录一段关于家庭菜的真实语音，并选择允许 AI 在该内容范围内进行有限回应。女儿未来通过实体信物进入 App，在下雨天主动打开 Agent。系统找到一段与雨天和做饭有关的真实记忆，呈现原始语音与 AI 串联内容。互动结束后生成一张“今天收到的远行明信片”，女儿可以选择收藏、回应或关闭后续提示。

---

## 9. 软件技术架构

### 9.1 Domain Model

建议实体：

```text
User
ActorRole
LifeProfile
Relationship
Recipient
ContextItem
OriginalAsset
DerivedContent
AgentProfile
GenerationPolicy
TriggerPolicy
Interaction
InteractionArtifact
FeedbackPreference
HardwareEvent
```

### 9.2 ContextItem 建议字段

```text
id
subject_id
recorder_id
recipient_id
relationship_id
source_type
modality
capture_mode
original_asset_ref
transcript
topic
meaning
emotion_label
emotion_intensity
importance_weight
sensitivity_level
visibility
ai_policy
trigger_policy
intended_scenarios
provenance
created_at
updated_at
```

### 9.3 Agent 输出要求

每次生成结果至少返回：

```text
output_type
content
source_context_ids
generation_mode
ai_generated
confidence
sensitivity
trigger_reason
user_controls
```

### 9.4 API

黑客松提供了可高频调用的 API 配额，计划通过 OpenCode 调用。

要求：

- API Key 只使用环境变量；
- 统一 Adapter；
- 支持 mock；
- 记录请求和错误；
- 缓存重复分析；
- 保存结构化输出；
- 不把“额度无限”理解为可以无限重复调用同一任务。

---

## 10. 本地协作环境

项目主目录：

```text
D:\Codex-Workspace\Loop
```

协作方式：

- Codex 桌面版：总览、任务拆解、架构审查、状态同步、集成判断；
- OpenCode：实际开发、测试和提交；
- 多个 OpenCode 窗口并行执行独立任务；
- 所有窗口共享项目事实文件，不共享模糊口头假设。

推荐目录：

```text
.loop/
  STATUS.md
  DECISIONS.md
  RISKS.md
  INTEGRATION_QUEUE.md
  tasks/
  claims/
  reports/
```

原则：

> 一个窗口，一个任务，一个分支，一份报告。

---

## 11. 当前优先级

### P0：必须完成

- 用户、记录者、接收者与关系模型；
- 主动 Context 录入与关系化引导；
- 原始内容和 AI 派生层分离；
- Context 编辑和人工审核；
- 对象专属 Agent；
- 有来源、有授权的有限演绎；
- 用户主动进入的接收者体验；
- 一张远行明信片式互动结果；
- Hardware Simulator；
- 端到端 Demo 和离线 fallback。

### P1：有时间再做

- 纪念日 / 天气等 Trigger Engine；
- 用户成长与反馈偏好；
- 共同计划；
- 多模态输入；
- 真实戒指或 NFC 接入。

### P2：暂缓

- HRV 情绪识别；
- 全自动无感采集；
- 强主动干预；
- 家庭成员协作网络；
- 完整一对多托付；
- 高自由度人格克隆；
- 复杂游戏系统。

---

## 12. 一句话版本

> Loop 让一个人在仍能表达时，亲自整理自己与某个人的真实关系，并把这段关系以有来源、有边界、有仪式的方式留给未来。
